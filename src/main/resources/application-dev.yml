# Spring 数据源 (DataSourceAutoConfiguration & DataSourceProperties)
spring:
  datasource:
    url: jdbc:mysql://39.100.67.97:32311/user_app?useSSL=false&&serverTimezone=GMT%2B8
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect # Hibernate 属性，SQL 方言使得 Hibernate 为所选数据库生成更好的 SQL
  jackson:
    serialization:
      write-dates-as-timestamps: true